spring:
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: 8500
      config:
        enabled: true
        prefix: config
        defaultContext: recommend
        data-key: data
        format: YAML
        watch:
          wait-time: 1
          delay: 1000
        fail-fast: true
