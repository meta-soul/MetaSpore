apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movielens-chart.fullname" . }}-test-connection"
  labels:
    {{- include "movielens-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movielens-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
